# FN-20260226-0049: 메모 TODO 반영 동작 수정

## Original Task Description
- "TODO에 반영" 누르면 memo.md 비우기가 안됨 — 정상적으로 비우도록 수정
- 반영 누르면 메모 뷰어가 닫히고, TODO 뷰어가 열리도록 변경
- 각 시점(저장/닫기/열기)에 딜레이를 추가하여 파일 저장 경합(동시 쓰기, 덮어쓰기) 방지

## Summary of Work Done
- `confirmApplyToTodo` 핸들러에서 `handleSave()` 사전 호출을 제거하고, 웹뷰 마크다운을 `handleApplyToTodo(markdown)`에 직접 전달하도록 변경
- 파일에서 읽는 대신 전달받은 마크다운을 직접 파싱하여 파일 I/O 경합 제거
- todo.md 저장 후 memo.md 비우기 전(150ms), 비우기 확인(100ms), 뷰어 전환 전(300ms)에 딜레이 추가
- memo.md 비우기 후 재확인하여 내용이 남아있으면 재시도
- 웹뷰 새로고침 대신 `dispose()`로 메모 뷰어를 닫고 `TodoViewerEditor.openOrCreate()`로 TODO 뷰어 열기

## Related Devlog
- **Date**: 2026-02-26
- **Devlog ID**: 117
- **Detail File**: `devlog/v1.3.2/117-memo-todo-apply-fix.md`


# Review
- 확인함
